git init 
git clone 
git checkout (branch name) 
git checkout main 
git status 
git add (file name )
git commit 



git add . (to add all the files in the direcotry) (after we make changes to all the files)

// to commit with message use the below command 
git commit -m "message"



git log (see what changes you have made) 
ls -a (to see the hidden files too)

branching 

branch feature 
checkout feature 
edit , test , add , commit 
merge master 
test 
checkout master 
merge feature 

git diff ( to see all the changes we have made )


git clone 
git fetch 
git pull 

to view the hideen files use ls -a 

git push harsha <link>
git push -u origin master


git remote add origin <link>
git push -u origin master
